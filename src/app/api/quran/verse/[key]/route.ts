import { NextResponse } from "next/server";
import { z } from "zod";
import { getQuranFoundationVerseStudy, isQuranFoundationConfigured } from "@/lib/islamic/providers/quran/quran-foundation";
const schema = z.object({ key: z.string().regex(/^(?:[1-9]\d?|1[01]\d|114):[1-9]\d{0,2}$/), tafsir: z.coerce.number().int().positive().optional(), translation: z.coerce.number().int().positive().optional() });
export async function GET(request: Request, { params }: { params: Promise<{ key: string }> }) { if (!isQuranFoundationConfigured()) return NextResponse.json({ error: "Quran Foundation غير مهيأ" }, { status: 503 }); const { key } = await params; const url = new URL(request.url); const parsed = schema.safeParse({ key, tafsir: url.searchParams.get("tafsir") || undefined, translation: url.searchParams.get("translation") || undefined }); if (!parsed.success) return NextResponse.json({ error: "طلب غير صالح" }, { status: 400 }); try { return NextResponse.json(await getQuranFoundationVerseStudy(parsed.data.key, parsed.data.tafsir, parsed.data.translation), { headers: { "Cache-Control": "public, s-maxage=86400, stale-while-revalidate=604800" } }); } catch { return NextResponse.json({ error: "تعذر تحميل التفسير أو الترجمة" }, { status: 502 }); } }
