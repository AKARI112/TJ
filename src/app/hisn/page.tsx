import { HisnIndex } from "@/components/devotional/hisn-index";
import { getHisnChapters } from "@/lib/islamic/providers/adhkar/hisn";
export const metadata = { title: "حصن المسلم", description: "فهرس حصن المسلم بالنص العربي والمراجع، مع بحث وحفظ ومشاركة." };
export default async function HisnPage() { const chapters = await getHisnChapters(); const provider = chapters[0]?.provider ?? "Islamic App"; const sourceUrl = chapters[0]?.sourceUrl ?? "https://docs.islamic.app/api-reference/dhikr"; return <main><header className="page-intro"><p className="eyebrow text-accent">حصن المسلم</p><h1>الكتاب، بابًا بعد باب</h1><p>{chapters.length.toLocaleString("ar-SA")} بابًا من {provider}، وتُحمّل نصوص الباب عند فتحه مع نسخة محلية احتياطية للأوفلاين.</p></header><HisnIndex chapters={chapters} /><footer className="content-attribution"><span>المصدر المستخدم: {provider}</span><a href={sourceUrl} target="_blank" rel="noreferrer">عرض المصدر</a></footer></main>; }
