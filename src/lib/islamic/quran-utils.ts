export function isMushafPage(value: number) { return Number.isInteger(value) && value >= 1 && value <= 604; }
export function qcfFontUrls(page: number, mode: "mushaf" | "tajweed") { if (!isMushafPage(page)) throw new RangeError("صفحة المصحف بين 1 و604"); return mode === "tajweed" ? { primary: `https://verses.quran.foundation/fonts/quran/hafs/v4/colrv1/woff2/p${page}.woff2`, darkFallback: `https://verses.quran.foundation/fonts/quran/hafs/v4/ot-svg/dark/woff2/p${page}.woff2`, lightFallback: `https://verses.quran.foundation/fonts/quran/hafs/v4/ot-svg/light/woff2/p${page}.woff2`, mushafId: 19 as const } : { primary: `https://verses.quran.foundation/fonts/quran/hafs/v2/woff2/p${page}.woff2`, mushafId: 1 as const }; }
export function preserveQuranText(value: string) { return value.normalize("NFC"); }
export function normalizeResource(input: { id: number | string; name?: string; language_name?: string; author_name?: string }) { return { id: Number(input.id), name: input.name?.trim() || String(input.id), languageName: input.language_name?.trim() || undefined, authorName: input.author_name?.trim() || undefined }; }
